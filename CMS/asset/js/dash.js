'use strict';

angular.module('my-app')

/**
 * @ngdoc controller
 * @name RdvngSearchOptions
 * @requires 
 * @description
 * Controller for the default document page
 */

.controller("dashboardCtrl", function($scope) {
    $scope.tenantsli == "hello world";

   /* $scope.addList = function() {
        $scope.tenantsli.push({
            'name': $scope.listName,
            'data': $scope.listData
        });

    }*/
});
